# Complete requirements file - includes all dependencies for full development environment

# Include API requirements (which includes base)
-r requirements-api.txt

# Include crawler requirements (which includes base + PDF/OCR libraries)
-r requirements-crawler.txt

# This approach avoids duplication while ensuring all dependencies are available
# For production deployments:
# - Use requirements-api.txt for API containers (lightweight)
# - Use requirements-crawler.txt for document processing containers
# - Use this file for local development with full capabilities

# Development/Testing only dependencies
pytest-cov==5.0.0            # Coverage reporting for pytest
coverage-badge==1.1.2        # Generate SVG coverage badge from .coverage data

# Ops scripts (scripts/create_snapshots.py)
zstandard==0.23.0            # Multi-threaded zstd compression for snapshot tarballs
