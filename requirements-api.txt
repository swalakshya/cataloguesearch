# API-only requirements (lightweight for containerized API service)

# Include base requirements
-r requirements-base.txt

## PDF export (Aagam Khoj "Export to PDF")
weasyprint==69.0            # HTML/CSS -> PDF rendering with correct Devanagari/Gujarati shaping (needs system Pango/Cairo libs, see docker/api/Dockerfile)

# This file exists to explicitly separate API dependencies from crawler dependencies